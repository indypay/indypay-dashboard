const OperationsNavigations = () => {
  // const pathname = usePathname();

  // return (
  //     <nav>
  //         {OperationsTabs.map((tab) => (
  //             <Link
  //                 key={tab.id}
  //                 href={`/operations${tab.value}`}
  //                 className={pathname?.includes(tab.value) ? 'active' : ''}
  //             >
  //                 {tab.label}
  //             </Link>
  //         ))}
  //     </nav>
  // );
  return null;
};

export default OperationsNavigations;
